import { TestSelectDirective } from './text-select.directive';

describe('TestSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new TestSelectDirective();
    expect(directive).toBeTruthy();
  });
});
